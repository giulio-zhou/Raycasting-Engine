Raycasting-Engine
=================

Compile Engine.java using javac
Run using >>> java Engine <height> <width> 
	to specify the desired height and width of the window
In terminal, displays a text-based map editor, input "n" to continue editing.
Once the JFrame window is opened, use wasd (up/left/down/right) controls to move.
